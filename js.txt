<!-- JS динамиечски типизированный язык -->
    а = 10
    а = "абс" 
    не нужно указывать стринг или намбер



<!-- ОБЪЕКТЫ -->
    имя(ключ):значение
    const myCity = {mostPopular: "Tomsk"}
        myCity.nowCity = 'Lensk' добавили новый ключ значение в переменную
        myCity.nowCity = 'Yakutsk' изменили свойство
        delete myCity.nowCity удалили свойство (с помощью оператора)
        <!--квадратные скобки 
        myCity['popular']= true

<!-- Глобальные объекты -->
    window браузер
    global nodeJS
    blobalThis бразуер+нодаДЖС

<!-- МЕТОД -->
свойство объекта значение которого функция
(нужно добавить круглые скобки)



<!-- МУТАЦИИ -->



<!-- ФУНКЦИИ (объект)-->
    Блок кода который можно выполнять многократно
    могут быть:
    - Именнованной (functio sum (a,b) { a + b })
    - присвоена Переменной
    - анонимной
    - аргументом при вызове ФУНКЦИИ
    - занчением свойства (метода) объекта



<!-- ОПЕРАТОРЫ -->
    встроенная функция
    сравнения, Арифмет, Логические, присваивания
     Унарные 
        ! (не)
        а++
        ++а (прибавить 1)
        +а (конвертировать строку в число)
        delete (удалить)
        typeof (типо переменной)
        new Object (создаст новый экземпляр объекта).
     Бинарные 
        а+=5 (увеличить значени на 5)
        == (значения)
        === (сравнения тип значения)
        && (и) возвращает первое если ложь и второе если правда
        || (или) возвращает первое правдивое